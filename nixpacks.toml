[phases.setup]
nixpkgs = ['python3', 'pip']

[build]
image = 'python'
command = "pip install -r requirements.txt"

[phases.run]
command = "pip install flask && pip install -r requirements.txt && flask run --host=0.0.0.0 --port=5000"
